# Copy to mysql.env (gitignored). Used only as Docker Compose env_file — safe for passwords with $.
# PowerShell: Copy-Item mysql.env.example mysql.env

MYSQL_DATABASE=db_agent_test
MYSQL_ROOT_PASSWORD=change-me-root-strong
AGENT_USER_PASSWORD=change-me-agent-strong
